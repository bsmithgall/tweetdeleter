{
  "name": "tweetdeleter",
  "version": "0.1.0",
  "description": "Delete ur tweets",
  "main": "index.js",
  "scripts": {
    "start": "npm run build:live",
    "build": "tsc -p .",
    "build:watch": "nodemon --watch 'src/**/*.ts' --exec 'ts-node'",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^11.13.7",
    "@types/request-promise": "^4.1.43",
    "dotenv": "^7.0.0",
    "nodemon": "^1.18.11",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.5"
  },
  "dependencies": {
    "@types/json2csv": "^4.4.0",
    "commander": "^2.20.0",
    "json2csv": "^4.5.1",
    "request": "^2.88.0",
    "request-promise": "^4.2.4",
    "request-promise-native": "^1.0.7"
  }
}
